#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

/* === Решение задачи: ===

Это отличная задача: она объединяет в себе сразу три мощные концепции: строковые алгоритмы (префикс-функция и бордеры),
комбинаторику (вывод линейной рекурренты) и продвинутую алгебру (вычисление K-го члена рекурренты за O(n \log n \log K) с помощью быстрого
преобразования Фурье и алгоритма Бостана-Мори).

----------------------------

# 1. Математический разбор идеи

**Шаг 1: Определяем состояния**
Пусть $n$ — длина нашей строки $s$, а $A = 26$ — размер алфавита.
Введем две последовательности:
* $g_k$ — количество «хороших» строк длины $k$. (хорошая строка — это та, которая заканчивается на $s$, и при этом $s$ больше нигде в
  ней не встречается, в задаче мы именно их количество и ищем).
* $h_k$ — количество строк длины $k$, в которых подстроки $s$ нет вообще.

**Шаг 2: Связываем $g$ и $h$**
Как получить строку, заканчивающуюся на $s$? Нужно взять строку длины $k-n$, в которой нет $s$ (их ровно $h_{k-n}$), и приписать к ней $s$ в конец. 
Но гарантирует ли это, что мы получим «хорошую» строку? Нет! В месте "склейки" старой строки и приписанной $s$ могла образоваться еще одна,
более ранняя подстрока $s$.

Если это произошло, то наша добавленная $s$ пересекается с "нелегальной" свое копией, начинающейся пораньше (то есть префикс приписанной $s$ пересекается
с суффиксом себя же, расположенной раньше). Пересечение префикса и суффикса строки называется **бордером** (border).
Пусть $B$ — множество длин всех собственных (то есть не равных всей строке) бордеров строки $s$ (например, для $s = \text{"aba"}$, длина бордера равна 1,
так как префикс $\text{"a"}$ совпадает с суффиксом $\text{"a"}$). Получается, что при приписывании $s$ в конец, вся строка могла стать «хорошей» чуть
раньше — на позиции $k - n + p$, где $p$ — длина бордера. 

Отсюда получаем формулу:
$$h_{k-n} = g_k + \sum_{p \in B} g_{k - n + p}$$
Или, если выразить $g_k$:
$$g_k = h_{k-n} - \sum_{p \in B} g_{k - n + p} \quad \text{--- (Уравнение 1)}$$

**Шаг 3: Избавляемся от $h$**
Мы знаем еще один простой факт: если к строке длины $k-1$ без $s$ (их $h_{k-1}$) приписать любую из 26 букв, она либо останется строкой без $s$,
либо превратится в «хорошую» строку длины $k$.
$$26 \cdot h_{k-1} = h_k + g_k \implies h_k - 26 \cdot h_{k-1} = -g_k$$
Заменим индексы на $k-n$:
$$h_{k-n} - 26 \cdot h_{k-n-1} = -g_{k-n} \quad \text{--- (Уравнение 2)}$$

Теперь берем Уравнение 1 для $g_k$ и вычитаем из него Уравнение 1 для $26 \cdot g_{k-1}$. После группировки и подстановки Уравнения 2 (это чисто
алгебраические преобразования) мы получаем чистую рекурренту только для $g$:
$$g_k = 26 \cdot g_{k-1} - g_{k-n} - \sum_{p \in B} g_{k-n+p} + 26 \cdot \sum_{p \in B} g_{k-n-1+p}$$

**Шаг 4: Как это считать для $m \le 10^9$?**
У нас есть линейная рекуррента порядка $n$. Считать ее по шагам — это $O(n \cdot m)$, что даст Time Limit. Классическое возведение матрицы в
степень даст $O(n^3 \log m)$, что тоже слишком медленно для $n = 10^5$.

Здесь на сцену выходит производящая функция и **алгоритм Бостана-Мори** (Bostan-Mori). 
Поскольку $g_k$ строится так, что $g_0 = \dots = g_{n-1} = 0$, а $g_n = 1$, наша последовательность — это в точности коэффициенты разложения
рациональной дроби $\frac{1}{Q(x)}$, где $Q(x)$ — многочлен, составленный из коэффициентов нашей рекурренты: $Q(x) = 1 - \sum c_i x^i$.
Чтобы найти $g_m$, нам нужно вычислить коэффициент при $x^{m-n}$ в разложении $\frac{1}{Q(x)}$. Алгоритм Бостана-Мори делает это за $O(n \log n \log m)$,
используя быстрое преобразование Фурье по модулю (NTT) для перемножения многочленов.


----------------------------

# 2. Более подробные пояснения некоторых мест решения.

### 1. Что такое бордер и откуда берется формула?

**Формальное определение бордера:**
Пусть у нас есть строка $s$ длины $n$ (индексация от $0$ до $n-1$). 
**Бордер** длины $p$ ($0 < p < n$) — это ситуация, когда префикс длины $p$ в точности совпадает с суффиксом длины $p$. 
Формально: $s[0 \dots p-1] = s[n-p \dots n-1]$. 
*(Заметим: $p$ строго меньше $n$, саму строку мы своим собственным бордером не считаем).*

**Разбор формулы: $$h_{k-n} = g_k + \sum_{p \in B} g_{k - n + p}$$**

В разборе выше может возникнуть отличный вопрос, не нужно ли проверять, что $h$ не любая, а действительно где есть окончание на префикс...
Однако вся магия комбинаторики здесь строится на **биекции** (взаимно однозначном соответствии). Мы не строим строку слева направо,
проверяя условия, мы берем множество *всех* строк $h_{k-n}$ и приписываем к каждой в конец строку $s$ (длина которой $n$). 

Давайте посмотрим на получившееся множество строк длины $k$. В каждой из них строка $s$ точно есть (мы же её только что приписали в конец).
Теперь возьмем любую такую строку и найдем в ней **самое первое** вхождение $s$. Где оно может заканчиваться?

1.  **Случай 1: Оно заканчивается в самом конце (индекс $k$).**
    Это означает, что до самого конца строки $s$ нигде не встречалась. По определению это «хорошая» строка длины $k$.
    Таких строк ровно $g_k$.
2.  **Случай 2: Оно заканчивается раньше (индекс $k - n + p$, где $p > 0$).**
    Так как в изначальной левой части (длины $k-n$) строки $s$ не было (это же $h_{k-n}$), то это раннее вхождение обязано "залезть" на приписанную
    нами $s$. Оно начинается где-то в левой части, а заканчивается внутри приписанной $s$, зацепив ровно $p$ её символов.
    Что такое эти $p$ символов?
    * С одной стороны, это первые $p$ символов приписанной строки $s$, то есть её **префикс** длины $p$.
    * С другой стороны, это конец "ранней" строки $s$, то есть её **суффикс** длины $p$.
    Получается, префикс $s$ совпал с суффиксом $s$! Это означает, что $p$ **обязано** быть длиной бордера строки $s$. Если $p$ не бордер,
    такое пересечение физически невозможно.
    
    Сколько таких строк? Так как это *самое первое* вхождение $s$, то часть строки от начала и до $k - n + p$ является «хорошей» строкой
    (их $g_{k-n+p}$). А что идет после неё? А после неё идут жестко заданные оставшиеся $n-p$ символов строки $s$. У нас нет выбора.
    Поэтому каждому вхождению на бордере $p$ соответствует ровно $g_{k-n+p}$ вариантов начала.

Так как каждое приписывание $s$ к $h_{k-n}$ дает ровно одно **самое первое** вхождение, сумма всех этих взаимоисключающих случаев в точности равна
общему числу исходных строк $h_{k-n}$.



### 2. Что такое P/Q и как получается рекуррента? (Производящие функции)

Это переход из дискретной математики в непрерывную алгебру. 
Представим, что у нас есть бесконечная последовательность чисел (наша рекуррента): $a_0, a_1, a_2, a_3, \dots$
Мы можем «упаковать» всю эту бесконечную последовательность в одну единственную функцию, сделав эти числа коэффициентами многочлена:
$$A(x) = a_0 + a_1 x + a_2 x^2 + a_3 x^3 + \dots$$
Эта штука называется **производящей функцией** (Generating Function).

**Почему P/Q?**
Оказывается, любая линейная рекуррента сворачивается в дробь двух конечных многочленов!
Посмотрим на примере чисел Фибоначчи: $F_k = F_{k-1} + F_{k-2}$ (где $F_0 = 0, F_1 = 1$).
Запишем функцию:
$$F(x) = 0 + 1x + 1x^2 + 2x^3 + 3x^4 + 5x^5 + \dots$$
А теперь умножим её на $x$ и на $x^2$:
$$x F(x) = 0 + 0x + 1x^2 + 1x^3 + 2x^4 + 3x^5 + \dots$$
$$x^2 F(x) = 0 + 0x + 0x^2 + 1x^3 + 1x^4 + 2x^5 + \dots$$

Если мы вычтем из первой строчки вторую и третью, то, благодаря рекурренте ($F_k - F_{k-1} - F_{k-2} = 0$), почти все коэффициенты обнулятся!
$$F(x) - x F(x) - x^2 F(x) = x$$
$$F(x) (1 - x - x^2) = x \implies F(x) = \frac{x}{1 - x - x^2}$$

Вот оно! 
* Знаменатель $Q(x) = 1 - x - x^2$ — это коэффициенты нашей рекурренты (с минусами).
* Числитель $P(x) = x$ — это многочлен, зависящий от базовых (начальных) значений рекурренты.

**Что значит "поделить"?**
Если взять $x$ и столбиком поделить на $1 - x - x^2$ по правилам деления многочленов, ты в ответе начнет получаться бесконечный
ряд: $x + x^2 + 2x^3 + 3x^4 \dots$, коэффициенты которого и есть числа Фибоначчи! То есть $K$-й член рекурренты — это просто коэффициент
при $x^K$ при делении $P(x)$ на $Q(x)$.



### 3. Идейная магия алгоритма Бостана-Мори

Итак, нам нужно найти коэффициент при $x^K$ в дроби $\frac{P(x)}{Q(x)}$. Если $K = 10^9$, делить столбиком или считать по шагам долго.
Бостан и Мори придумали гениальный трюк: как на каждом шаге **уменьшать $K$ в два раза**.

1. **Домножение на сопряженное:**
   Умножим числитель и знаменатель дроби на $Q(-x)$ (это тот же $Q(x)$, но у нечетных степеней $x$ изменен знак).
   $$\frac{P(x)}{Q(x)} = \frac{P(x) \cdot Q(-x)}{Q(x) \cdot Q(-x)}$$

2. **Свойство знаменателя:**
   Если умножить $Q(x)$ на $Q(-x)$, то в результате **все нечетные степени $x$ исчезнут**. Останутся только четные. 
   Например, $(1-x)(1+x) = 1 - x^2$. 
   Значит, новый знаменатель можно записать как какую-то функцию только от четных степеней: $V(x^2)$.

3. **Разделение числителя:**
   Новый числитель $U(x) = P(x) \cdot Q(-x)$ мы можем разделить на две части: четные степени и нечетные степени.
   $U(x) = U_{\text{чет}}(x^2) + x \cdot U_{\text{нечет}}(x^2)$

4. **Две независимые дроби:**
   Теперь наша дробь выглядит так:
   $$\frac{P(x)}{Q(x)} = \frac{U_{\text{чет}}(x^2)}{V(x^2)} + x \frac{U_{\text{нечет}}(x^2)}{V(x^2)}$$
   Получается красиво: левая дробь содержит **только четные** степени $x$, а правая дробь содержит **только нечетные** степени $x$!

5. **Шаг уменьшения $K$:**
   * Если нам нужен был четный коэффициент (например, $K = 100$), мы точно знаем, что правая дробь на него никак не влияет.
     Значит, мы можем забыть про правую дробь, взять левую $\frac{U_{\text{чет}}(x^2)}{V(x^2)}$, сделать замену переменной
     $t = x^2$ и искать в новой дроби $\frac{U_{\text{чет}}(t)}{V(t)}$ коэффициент под номером $K / 2 = 50$.
   * Если $K$ нечетное (например, $101$), то левая часть не нужна. Берем правую, ищем в ней коэффициент $(K-1)/2 = 50$.

Мы за одно перемножение многочленов (которое быстро делается через NTT) уменьшили искомый индекс $K$ ровно в два раза.
За $\approx 30$ шагов мы доберемся от $K = 10^9$ до $K = 0$, где ответ — это просто свободный член числителя.
*/

using namespace std;

static constexpr int MOD = 998'244'353;

// =========================================================================
// БЛОК 1: БЫСТРОЕ ПРЕОБРАЗОВАНИЕ ФУРЬЕ (NTT)
// Для быстрого умножения многочленов за O(N log N)
// =========================================================================

// Быстрое возведение в степень по модулю
int power(int base, long long exp) {
    int res = 1;
    base %= MOD;
    while (exp > 0) {
        if (exp % 2 == 1) res = (1LL * res * base) % MOD;
        base = (1LL * base * base) % MOD;
        exp /= 2;
    }
    return res;
}

// Обратный элемент в кольце по модулю
int modInverse(int n) { 
    return power(n, MOD - 2); 
}

// Сама функция NTT (Number Theoretic Transform)
// (обычное Быстрое Преобразование Фурье, но над целыми числами по модулю)
void ntt(vector<int>& a, bool invert) {
    int n = a.size();
    // Бит-реверсивная перестановка
    for (int i = 1, j = 0; i < n; i++) {
        int bit = n >> 1;
        for (; j & bit; bit >>= 1) j ^= bit;
        j ^= bit;
        if (i < j) swap(a[i], a[j]);
    }
    // Процесс "бабочки"
    for (int len = 2; len <= n; len <<= 1) {
        // Первообразный корень для 998244353 равен 3
        int wlen = power(3, (MOD - 1) / len); 
        if (invert) wlen = modInverse(wlen);
        for (int i = 0; i < n; i += len) {
            int w = 1;
            for (int j = 0; j < len / 2; j++) {
                int u = a[i+j], v = (1LL * a[i+j+len/2] * w) % MOD;
                a[i+j] = u + v < MOD ? u + v : u + v - MOD;
                a[i+j+len/2] = u - v >= 0 ? u - v : u - v + MOD;
                w = (1LL * w * wlen) % MOD;
            }
        }
    }
    // Нормализация при обратном преобразовании
    if (invert) {
        int n_inv = modInverse(n);
        for (int& x : a) x = (1LL * x * n_inv) % MOD;
    }
}

// Умножение двух полиномов P(x) * Q(x). Коэффициенты многчленов (от младшего по 0-му индексу к старшему) записаны в векторы a и b:
vector<int> multiply(vector<int> const& a, vector<int> const& b) {
    if (a.empty() || b.empty()) return {};
    vector<int> fa(a.begin(), a.end()), fb(b.begin(), b.end());
    int n = 1;
    while (n < a.size() + b.size())
        n <<= 1; // Округляем размер до степени двойки
    fa.resize(n); fb.resize(n);
    
    ntt(fa, false); ntt(fb, false); // Переходим в пространство значений нашим преобразованием
    for (int i = 0; i < n; i++)
        fa[i] = (1LL * fa[i] * fb[i]) % MOD; // Умножаем полиномы по значению
    ntt(fa, true); // Возвращаемся обратно в пространство коэффициентов, получая коэффициента полинома-произведения
    
    vector<int> res(a.size() + b.size() - 1);
    for (int i = 0; i < res.size(); i++)
        res[i] = fa[i];
    
    // Срезаем ведущие нули, чтобы многочлены не разрастались лишним мусором
    while (res.size() > 1 && res.back() == 0)
        res.pop_back();
    return res;
}

// =========================================================================
// БЛОК 2: АЛГОРИТМ БОСТАНА-МОРИ
// Находит коэффициент x^K в разложении P(x)/Q(x) за O(N log N log K)
// =========================================================================
int bostan_mori(vector<int> P, vector<int> Q, long long K) {
    while (K > 0) {
        // Создаем Q(-x) — меняем знаки у коэффициентов при нечетных степенях
        vector<int> Q_minus = Q;
        for (int i = 1; i < (int)Q.size(); i += 2) {
            Q_minus[i] = (MOD - Q[i]) % MOD;
        }
        
        vector<int> next_P = multiply(P, Q_minus);
        vector<int> next_Q = multiply(Q, Q_minus);
        
        P.clear();
        // Если K четное, берем четные степени P(x). Если нечетное — нечетные.
        for (int i = K % 2; i < (int)next_P.size(); i += 2) {
            P.push_back(next_P[i]);
        }
        
        Q.clear();
        // В Q(x) * Q(-x) всегда выживают только четные степени
        for (int i = 0; i < (int)next_Q.size(); i += 2) {
            Q.push_back(next_Q[i]);
        }
        
        K /= 2;
    }
    return P.empty() ? 0 : P[0]; // Когда K = 0, ответ — это свободный член P(x) (так как Q(0)=1)
}

// =========================================================================
// ОСНОВНАЯ ЛОГИКА И ВЫЧИСЛЕНИЕ КОЭФФИЦИЕНТОВ РЕКУРРЕНТЫ
// =========================================================================
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    long long m;
    cin >> n >> m;
    string s;
    cin >> s;

    // 1. Считаем префикс-функцию для поиска бордеров
    vector<int> pi(n, 0);
    for (int i = 1; i < n; i++) {
        int j = pi[i - 1];
        while (j > 0 && s[i] != s[j]) j = pi[j - 1];
        if (s[i] == s[j]) j++;
        pi[i] = j;
    }

    // 2. Собираем длины всех бордеров строки s
    vector<int> borders;
    int len = pi[n - 1];
    while (len > 0) {
        borders.push_back(len);
        len = pi[len - 1];
    }

    // 3. Строим коэффициенты c_i для нашей рекурренты g_k = \sum c_i * g_{k-i}
    // Нам нужен массив размером n+2, чтобы избежать выхода за пределы
    vector<long long> c(n + 2, 0);
    
    c[1] = (c[1] + 26) % MOD;           // + 26 * g_{k-1}
    c[n] = (c[n] - 1 + MOD) % MOD;      // - g_{k-n}
    
    for (int p : borders) {
        // - g_{k - n + p}
        c[n - p] = (c[n - p] - 1 + MOD) % MOD; 
        // + 26 * g_{k - n - 1 + p}
        c[n + 1 - p] = (c[n + 1 - p] + 26) % MOD; 
    }

    // 4. Формируем полином знаменателя Q(x) = 1 - c_1*x - c_2*x^2 - ... - c_n*x^n
    vector<int> Q(n + 1, 0);
    Q[0] = 1;
    for (int i = 1; i <= n; i++) {
        Q[i] = (MOD - c[i]) % MOD; // вычитаем коэффициент (с учетом модуля)
    }

    // Полином числителя: так как g_n = 1 (и все предыдущие нули), P(x) = 1
    vector<int> P = {1};

    // 5. Ищем (m - n)-ый член нашей последовательности
    long long K = m - n;
    int ans = bostan_mori(P, Q, K);

    cout << ans << "\n";

    return 0;
}
